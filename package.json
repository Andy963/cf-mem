{
  "name": "cf-mem",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && npm run test:hook",
    "test:hook": "python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260812.1",
    "happy-dom": "^20.14.5",
    "typescript": "^5.3.3",
    "vitest": "^5.0.0",
    "wrangler": "^4.122.0"
  },
  "dependencies": {
    "jose": "^6.2.12"
  }
}
